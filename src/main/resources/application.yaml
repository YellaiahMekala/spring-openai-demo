spring:
  application:
    name: spring-openai-demo

  ai:
    openai:
      api-key: ${OPENAI_APIKEY}

    ollama:
      base-url: http://localhost:11434
      chat:
        model: llama3.2:1b

    chat:
      client:
        enabled: false

#    google:
#      genai:
#        api-key: ${GEMINI_API_KEY}

server:
  port: 8080
