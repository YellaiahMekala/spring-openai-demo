spring:
  application:
    name: spring-openai-demo

  ai:
    openai:
      api-key: ${OPENAI_APIKEY}

#    chat:
#      options:
#        model: gpt-4o-mini
#        temperature: 0.3
#        max_tokens: 500



    ollama:
      base-url: http://localhost:11434
      chat:
        model: llama3.2:1b

    chat:
      client:
        enabled: false
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG



#    google:
#      genai:
#        api-key: ${GEMINI_API_KEY}

server:
  port: 8080
